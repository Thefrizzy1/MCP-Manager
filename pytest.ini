[pytest]
# Repo root on sys.path so tests can import config / client / core.* directly.
pythonpath = .
testpaths = tests
addopts = -q
markers =
    live: hits real public endpoints over the network (skips gracefully if the
        network is unavailable). Deselect with -m "not live".
